#include <iostream>
using namespace std;

int main()
{
    // cout << "This is array chapter" << endl;

    // int scores[3] = {85, 90, 78};

    // for (size_t i = 0; i < 3; i++)
    // {
    //     cout << "Current score is: " << scores[i] << endl;
    // }

    // for (auto value : scores)
    // {
    //     cout << "Current score is: " << value << endl;
    // }

    // cout << "Size of the array is " << sizeof(scores) / sizeof(scores[0]) << endl;

    // char message[] = {'H', 'e', 'l', 'l', 'o'}; // This is not a c string as there is not null character
    // If a character array is null terminated, it's called C-string. (\0)
    // cout << "The message is " << message << endl;
    // cout << "The size of the message is " << sizeof(message) << endl;
    // char message2[] = {"Hello"};
    // cout << "This is second message " << message2 << endl;
    // cout << "The size of msg 2 is " << sizeof(message2) << endl;
    // cout << "Size of char is " << sizeof(char) << endl;
    // message[2] = 'E';
    // cout << "With for loop ";
    // for (auto c : message)
    // {
    //     cout << c;
    // }
    // cout << endl;
    // cout << "With normal " << message;
    // cout << "Length of string " << sizeof(message) / sizeof(char) << endl;

    // // String literal
    // char message2[] = "Abhradip Paul";
    // cout << "My name is " << message2;
    // cout << "Unbounded data " << message2[12];
    cout << "Array handling";

    return 0;
}